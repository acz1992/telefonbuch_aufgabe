import { Typography } from "@mui/material";

const Title = () => {
	return (
		<Typography mt={2} textAlign="center" variant="h2">
			Phone Book
		</Typography>
	);
};

export default Title;
