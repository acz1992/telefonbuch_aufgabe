import { Typography } from "@mui/material";

const Title = () => {
	return (
		<Typography my={2} textAlign="center" variant="h1">
			Phone Book
		</Typography>
	);
};

export default Title;
